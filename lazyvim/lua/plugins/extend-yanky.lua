return {
  "gbprod/yanky.nvim",
  opts = {
    -- Create which-key namespace for <Space>p
    -- Remap <Space>ph to open yanky.nvim history (<Space>p by default)
    -- Set <Space>pp to put after from system clipboard
    -- Set <Space>pP to put before from system clipboard
  },
}
